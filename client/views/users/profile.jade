h3 PROFILE

.row
  .col-xs-4
    form(name='userForm', novalidate)
      .form-group
        label(for='email') Email
        input.form-control#email(type='email', name='email', ng-model='user.email')
      .form-group
        label(for='avatar') Avatar
        input.form-control#avatar(type='url', name='avatar', ng-model='user.avatar')
      .form-group
        button.btn.btn-default(type='submit', ng-click='update(user)') Update
  .col-xs-2
    img.img-responsive.img-thumbnail(ng-src='{{user.avatar}}')
  .col-xs-6
